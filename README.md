This code is all about the monthly challenges for each month.

Install Django using 'pip install django'

To start, Python manage.py runserver - to start the server on your local host

Open the localhost link/challenges

Navigate through the months
